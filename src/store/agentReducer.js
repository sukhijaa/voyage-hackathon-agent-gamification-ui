const initialState = {
    profile: {},
    rewardHistoru: [],
    milestones: []
}

const agentReducer = (state = initialState, action) => {
    switch (action.type) {
        default: 
        return state
    }
}

export default agentReducer